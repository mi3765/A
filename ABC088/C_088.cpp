#include <bits/stdc++.h>
using namespace std;
int main() {
    vector<vector<int> c(3, vector<int>(3));
    vector<int> a(3);
    vector<int> b(3);
    a[0] + b[0] = c[0][0];
    a[1] + b[1] = c[1][1];
    a[2] + b[2] = c[2][2];

    return 0;
}